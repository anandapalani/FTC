apiVersion: v1
kind: Service
metadata:
  name: product-registry-svc
  labels:
    app: product-registry
spec:
  type: NodePort
  selector:
    app: product-registry
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30080
